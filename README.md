# 21-IDUM School Website

Школьный веб-сайт на Node.js/Express с локальной JSON базой данных (lowdb).

## 🚀 Технологии

- **Backend**: Node.js + Express.js
- **Database**: lowdb (JSON файл)
- **Frontend**: HTML, CSS, JavaScript
- **Session Management**: express-session

## 📋 Требования

- Node.js (v14 или выше)
- npm или yarn

## 🛠️ Установка

1. **Установите зависимости:**
```bash
npm install
```

2. **Настройте файл .env:**

Отредактируйте файл `.env` и установите свои параметры:

```env
PORT=3000
SESSION_SECRET=случайная_строка_для_безопасности
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ваш_админ_пароль
```

## 🏃 Запуск

### Режим разработки (с автоперезагрузкой):
```bash
npm run dev
```

### Обычный режим:
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## 📁 Структура проекта

```
21-IDUM/
├── routes/
│   ├── auth.js            # Маршруты аутентификации
│   └── news.js            # Маршруты для новостей
├── views/
│   ├── index.html         # Главная страница
│   └── admin/
│       ├── login.html     # Страница входа
│       ├── dashboard.html # Главная админки
│       ├── add-news.html  # Добавление новости
│       └── edit-news.html # Редактирование новости
├── public/
│   ├── style.css          # Стили
│   └── images/            # Изображения
├── database.js            # Конфигурация lowdb
├── db.json                # База данных (JSON)
├── server.js              # Главный файл сервера
├── package.json           # Зависимости проекта
└── .env                   # Конфигурация окружения
```

## 🔑 API Endpoints

### Публичные endpoints:
- `GET /api/news` - Получить список новостей
  - Query параметры: `lang` (ru/uz), `limit` (число)
  - Пример: `/api/news?lang=ru&limit=10`

- `GET /api/news/:id` - Получить одну новость по ID
  - Query параметр: `lang` (ru/uz)

### Админ endpoints (требуют авторизации):
- `POST /api/auth/login` - Войти в систему
- `POST /api/auth/logout` - Выйти из системы
- `GET /api/auth/check` - Проверить авторизацию

- `POST /api/news` - Добавить новость
- `PUT /api/news/:id` - Обновить новость
- `DELETE /api/news/:id` - Удалить новость
- `GET /api/news/admin/all` - Получить все новости (все языки)

## 👤 Администрирование

1. Откройте `http://localhost:3000/admin/login`
2. Войдите с логином и паролем из `.env`
3. Управляйте новостями через админ-панель

## 🔒 Безопасность

- Пароли хранятся в переменных окружения (.env)
- Используются сессии для аутентификации
- CORS настроен для безопасности
- Данные хранятся локально в JSON файле

## 📝 Заметки

- База данных - это простой JSON файл (`db.json`)
- Не требуется установка MySQL
- Для продакшена рекомендуется использовать реальную БД
- Убедитесь, что `db.json` в `.gitignore` для безопасности
- Добавьте `.env` в `.gitignore`

## 🐛 Отладка

Если возникают проблемы:

1. Проверьте, что данные в `.env` корректны
2. Проверьте логи в консоли
3. Убедитесь, что порт 3000 не занят другим приложением
4. Убедитесь, что файл `db.json` существует и доступен для записи

## 📞 Поддержка

При возникновении вопросов проверьте логи сервера в консоли.
